{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "npx prisma generate --no-engine && nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@bg-dev/nuxt-auth": "2.2.12-edge",
    "@nuxt/devtools": "latest",
    "@types/node": "^20.9.0",
    "nuxt": "^3.8.2"
  }
}